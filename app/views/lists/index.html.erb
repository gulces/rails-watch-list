<div class="banner" style="background-image: linear-gradient(rgba(0,0,0,0.4),rgba(0,0,0,0.4)), url(https://cherwell.org/wp-content/uploads/2016/11/IMG_0243.jpg);">
  <div class="container">
    <h1>Save your <strong>favourite</strong> movies to <strong>watchlists</strong>!</h1>
    <p>Personalised movie guide at your fingertips.</p>
    <%= link_to "Create A List", "/lists/new", class: "btn btn-ghost" %>
    <%# <a class="btn btn-flat" href="#">Apply now</a> %>
  </div>
</div>


<div class="container">
  <h1>My Lists</h1>
  <%# <% @lists.each do |list| %>
    <%# <h4> <%= list.name </h4> %>
    <%# <%= cl_image_tag list.photo.key, height: 300, width: 400, crop: :fill %>
  <%# <% end %>

  <div class="container">
    <div class="row">
      <% @lists.each do |list| %>
      <div class="col-12 col-sm-6">
        <div class="card-category" style="background-image: linear-gradient(rgba(0,0,0,0.3), rgba(0,0,0,0.3)), url('<%= cl_image_path list.photo.key, height: 300, crop: :fill %>')">
          <%= link_to list.name, list_path(list) %>
        </div>
      </div>
      <% end %>
    </div>
  </div>



</div>
